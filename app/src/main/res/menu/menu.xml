<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context=".MainActivity">

    <item
        android:id="@+id/cancel_tracking_dialog"
        android:icon="@drawable/ic_clear_white_24dp"
        android:orderInCategory="100"
        android:title="@string/stop_tracking"
        android:onClick="stopTracking"
        app:showAsAction="always"/>
    <item
        android:id="@+id/show_add_tracking_dialog"
        android:icon="@drawable/ic_add_white_24dp"
        android:orderInCategory="101"
        android:title="@string/add_tracking"
        android:onClick="showAddTrackingDialog"
        app:showAsAction="always"/>
</menu>
